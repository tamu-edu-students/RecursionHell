{
  "scripts": {
    "start": "node server/server.js",
    "heroku-postbuild": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "mysql": "^2.18.1",
    "mysql2": "^2.3.3"
  }
}
