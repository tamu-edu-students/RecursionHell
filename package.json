{
  "scripts": {
    "start": "run-s server-install client-install start-app",
    "server-install": "npm install --prefix server",
    "client-install": "npm install --prefix frontend",
    "start-app": "run-p server client",
    "server": "node server/server.js",
    "client": "npm start --prefix frontend"
  },
  "dependencies": {
    "concurrently": "^7.4.0",
    "mysql": "^2.18.1",
    "mysql2": "^2.3.3",
    "npm-run-all": "^4.1.5"
  }
}
